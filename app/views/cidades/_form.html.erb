<%= form_for(@cidade) do |f| %>

  <%= render "shared/erros", objeto: f.object %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
